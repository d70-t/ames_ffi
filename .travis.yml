language: python
python:
    - 2.7
    - 3.3
    - 3.4
    - 3.5
    - nightly
sudo: false
install:
    - pip install coverage
    - pip install codecov
script:
    - nosetests --with-coverage --cover-package=ames --cover-branches test
    - coverage xml
after_success:
    - codecov
